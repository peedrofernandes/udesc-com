double maior (double a, double b)
{
  int m;
  
  if (a > b) {m = a;}
  else {m = b;}
  return b;
}

int fat (int n)
{
  int f;

  f = 0;

  while (n > 0)
  { 
    f = f * n;
    n = n - 1;
  }
}
   

void imprimir(string s, double r)
{
  int m;
  
  print (s);
  print (r);
  // return 0;
}

{
  int x, num;
  double a;
  // int x;
  
  print("Numero:");
  read (num);
  x = fat (4);
  a = maior (2.5, 10);
  imprimir("teste:", 2);
  return 0;
}
